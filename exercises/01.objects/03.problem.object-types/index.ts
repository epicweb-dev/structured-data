// Task Board
// Naming object types

// 🐨 Create a type called `Task` with:
// - id: number
// - title: string
// - completed: boolean

// 🐨 Create `firstTask` and `secondTask` using the Task type with these exact values:
// - firstTask: id 1, title "Write tests", completed false
// - secondTask: id 2, title "Ship release", completed true

// 🐨 Export both tasks so we can verify your work
// export { firstTask, secondTask }
