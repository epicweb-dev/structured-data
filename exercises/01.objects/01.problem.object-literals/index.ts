// User Management System
// Creating typed objects

// 🐨 Create a `user` object with these exact values:
// - name: 'Alice'
// - age: 30
// - email: 'alice@example.com'

// 🐨 Create a second object named `admin` with these exact values:
// - name: 'Bob'
// - age: 35
// - email: 'bob@example.com'

// console.log(`User: ${user.name}, Age: ${user.age}`)
// console.log(`Admin: ${admin.name}, Age: ${admin.age}`)

// 🐨 Export both objects so we can verify your work
// export { user, admin }
