# Object Literals

👨‍💼 We're building a user management system. We need to represent user data as
objects with proper types.

🐨 Open <InlineFile file="index.ts" /> and:

1. Create a `user` object with `name`, `age`, and `email` properties
2. Add an explicit type annotation for the object
3. Create a second user object using the same type structure

💰 Object type syntax:

```ts
const obj: { prop1: Type1; prop2: Type2 } = {
	prop1: value1,
	prop2: value2,
}
```

📜 [TypeScript Handbook - Object Types](https://www.typescriptlang.org/docs/handbook/2/objects.html)

🐨 Once you've completed the exercise, run `node index.ts` in the playground to test your work!
