# Enum vs Union Types

👨‍💼 Let's compare enums with union types. Both solve similar problems, but have
different tradeoffs.

```ts
// Enum approach
enum Color {
	Red = 'red',
	Green = 'green',
	Blue = 'blue',
}

// Union type approach
type Color = 'red' | 'green' | 'blue'
```

🐨 Open <InlineFile file="index.ts" /> and:

1. Implement the same feature using an enum
2. Implement the same feature using a union type
3. Compare the syntax and usage

💰 Union type syntax:

```ts
type HttpMethod = 'GET' | 'POST' | 'PUT' | 'DELETE'

function makeRequest(method: HttpMethod, url: string) {
	// ...
}

makeRequest('GET', '/api/users') // Works with string literals
```

📜 [TypeScript Handbook - Literal Types](https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#literal-types)

🐨 Once you've completed the exercise, run `node index.ts` in the playground to test your work!
