# Object Keys and Values

👨‍💼 We have a small inventory object and need to extract the names and
quantities for reporting.

🐨 Open <InlineFile file="index.ts" /> and:

1. Use `Object.keys` to get the item names
2. Use `Object.values` to get the item quantities
3. Compute the total quantity using the values array

💰 `Object.keys` gives you the property names. Keep ordering in mind when
pairing keys with values.

- 📜 [MDN - Object.keys](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/keys)
- 📜 [MDN - Object.values](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/values)
