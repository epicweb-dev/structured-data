# Map Lookups

👨‍💼 We need fast lookups by user ID, and `Map` is perfect for that.

🐨 Open <InlineFile file="index.ts" /> and:

1. Create a `Map` from the users array
2. Read a user from the map by ID
3. Check whether a missing ID exists

💰 You can build a map from entries: `new Map(users.map((user) => [user.id, user]))`.

📜 [MDN - Map](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map)
